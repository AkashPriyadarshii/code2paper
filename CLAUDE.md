# CLAUDE.md - code2paper

## What this is
- `code2paper` is a **universal agent skill** (`skills/code2paper.md`) — no CLI.
- It works in any AI agent: reads real code, measures real metrics (100+ languages),
  and produces `human-guide.md` + `paper.{md,typ,tex,html}`.

## Commands
- Install: copy `skills/code2paper.md` to your agent's skill dir.
- The skill is self-contained; there is nothing to build or test.

## Code Style
- The skill file is the product. Keep it self-contained, language-agnostic, honest.

## Workflow Rules
- R1-R5 contract active.
- Answer first, single recommendation.
